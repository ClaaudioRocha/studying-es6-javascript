// para declarar uma arrow function usando variaveis como parametro, precisamos se atentar a sintaxe! segue o exemplo abaixo.
const myConcat = (arr1, arr2) => arr1.concat(arr2);
console.log(myConcat(["café", "brownie"], ["capuccino", "chessecake"]));